name: GitlabSync
run-name: ${{ github.actor }} is testing out sync 🚀
on:
  - push
jobs:
  sync:
    runs-on: ubuntu-latest
    name: Git Repo Sync
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - uses: wangchucheng/git-repo-sync@v0.1.0
      with:
        target-url: https://repo.gviet.vn:1443/sigma-livestream/sigma-machine/sync
        target-username: congnt@gviet.vn
        target-token: rYH_i9oN6fj7v2FxyoSX