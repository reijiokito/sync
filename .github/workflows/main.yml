name: GitlabSync
run-name: ${{ github.actor }} is testing out sync 🚀
on:
  - push
jobs:
  sync:
    runs-on: ubuntu-latest
    name: Git Repo Sync 
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - uses: reijiokito/sync@main
      with:
        target-url: gitlab.com/reijiokito/ahihi.git
        target-username: reijiokito
        target-token: glpat-9XEKmQx8BeDHvNexj7DY
        
